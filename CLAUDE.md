# CLAUDE.md

DATUM STUDIO 2026 サマーインターン Team Astra（Team A）の開発・行動指針です。
AIエージェント（Snowflake CoCo / Claude Code）は、コード作成や変更を行う前に必ず本規約に従ってください。

---

## 1. Think Before Coding（実装前の思考と確認）
**推測で動かない。迷いを隠さない。トレードオフを言語化する。**
- 実装を始める前に、前提条件と変更計画を短く明記する。
- 複数の実装方針がある場合は、勝手に1つを選ばず選択肢を提示する。
- よりシンプルな方法がある場合は率直に提案し、過剰な設計にブレーキをかける。
- 不明点や曖昧な指示があれば立ち止まり、何が不明かを具体的に質問する。

## 2. Simplicity First（シンプルさ最優先・Karpathy原則）
**課題を解決する最小限のコードを書く。投機的な実装は一切しない。**
- 依頼されていない「将来のための拡張性」や「過剰な設定化・共通化」を作らない。
- 1度しか使わない処理を無理にクラス化・抽象化しない。
- 発生し得ないケースのための過剰なエラーハンドリングを書かない。
- 200行で書いたものが50行で書けるなら、50行に書き直す。
- 「シニアエンジニアが見て『無駄に複雑だ』と感じないか？」を常に自問する。

## 3. Surgical Changes（外科手術的なピンポイント変更）
**触るべき部分だけを触る。自分の変更による影響だけを片付ける。**
- 変更対象外の隣接コード、コメント、フォーマットを「ついでに改善」しない。
- 壊れていない既存ロジックを勝手にリファクタリングしない。
- 変更によって不要になった自分のimportや関数は必ず削除するが、既存の無関係なコードには触れない。
- すべての変更行が、ユーザーの要求に直接結びついている状態を維持する。

## 4. Goal-Driven & Verification（検証可能なゴール駆動）
**成功基準を定義し、ローカルで動作確認してから完了とする。**
- 「動くようにする」という曖昧な目標ではなく、検証可能なステップで進める。
- 各ステップごとに「何を実行してどうなれば合格か」を確認する。
- 画面実装時は、ローカル dev サーバーでランタイムエラーや Hydration エラーが出ていないことを確認する。

---

## 5. Snowflake App Runtime 開発規約
**公式スキル `/snowflake-apps` の流儀に必ず従うこと。**
- プロジェクトのスキャフォールディング、ディレクトリ構造、マニフェスト（`app.yml`）、Snowflake クエリヘルパー（`lib/snowflake.ts`）、およびデプロイ（`snow app deploy`）は、Snowflake 公式の `/snowflake-apps` スキルを最優先とし、その手順と規約に従う。
- 独自のNext.jsテンプレートや外部フレームワークでApp Runtimeの基本構成を上書き・バイパスしない。
- 大福帳データ（100万行規模）をクライアントにそのまま送らない。集計・フィルタ処理は必ず Snowflake 側で完結させ、パラメータ化クエリを使用する。

## 6. 環境方針 & Git ワークフロー
- **パッケージマネージャー**:
  - Pythonツール・Snowflake CLI: `uv`（`uv run snow ...`）を使用し、グローバル環境を汚さない。
  - Node.js / TypeScript: `bun`（`bun install`, `bun run dev`, `bunx`）を使用する。
- **Git 規約**:
  - `main` ブランチへの直接コミット・プッシュは禁止。必ず機能ブランチ（`feature/<name>`）で作業し、PRを作成する。
  - `.env`、認証鍵ファイル（`*.p8`）、Snowflake接続トークン、個人データのエクスポート結果は絶対にコミットしない。
- **安全第一の Snowflake 操作**:
  - 他チームのDB（`TEAM_B_DB`〜`TEAM_F_DB`）に一切触れない。
  - 開発・検証中の Snowflake 操作は原則 `SELECT`、`DESCRIBE`、`SHOW` 等の安全な読み取りを中心とし、オブジェクト作成・デプロイ時はターゲットスキーマをチーム内で確認してから行う。

